<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for AccessibilityHelper haptic feedback (vibrate() throws
         SecurityException without this). Added v1.5.3 after v1.5.1 shipped
         the haptics feature without the permission, crashing on mic/send tap. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".HandyAIApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.ClaudePhoneAgent">

        <meta-data android:name="io.sentry.auto-init" android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".OnboardingActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <activity
            android:name=".HubPairActivity"
            android:exported="false"
            android:label="@string/settings_hub"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".PanelPairActivity"
            android:exported="false"
            android:label="@string/settings_panel"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".KeySetupActivity"
            android:exported="false"
            android:label="@string/key_setup_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".EasyModeActivity"
            android:exported="false"
            android:label="@string/easy_mode_title"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".AgentAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
